package com.danielvdhaak;

import org.apache.spark.sql.*;

public class SparkUtils {
    /**
     * Explodes a one-column DataFrame containing list/array data and maps 
     * it to columns and rows.
     */
    public static void explodeDataFrame(Dataset<Row> df) {
        
    }
}
